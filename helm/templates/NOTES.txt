Thank you for installing {{ .Chart.Name }} ({{ .Chart.AppVersion }}).

Application URL:
{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else if contains "LoadBalancer" .Values.service.type }}
  Run the following to get the external IP:
  kubectl get svc --namespace {{ .Release.Namespace }} {{ include "app.fullname" . }}
{{- else if contains "NodePort" .Values.service.type }}
  export NODE_PORT=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "app.fullname" . }} -o jsonpath="{.spec.ports[0].nodePort}")
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo "http://$NODE_IP:$NODE_PORT"
{{- else }}
  kubectl port-forward --namespace {{ .Release.Namespace }} svc/{{ include "app.fullname" . }} 3000:{{ .Values.service.port }}
  Open: http://localhost:3000
{{- end }}

Health endpoint: /api/health
GraphQL endpoint: /api/graphql
REST hello endpoint: /api/hello
